// P2
#include<stdio.h>
#include<stdlib.h>

int suma(int n, int V[20])
{
	int i t = 0;
	for (i = 0; i <=n; i++)
	{
		k = k + V[i]
	}
	return k;
}
int main()
{
	int n, i, *V, S;
	printf("n=");
	scanf("%d",&n);
	V = (int *)malloc(n * sizeof(int));
	for (i = 0; i<n; i++)
	{
		printf("V[%d]=", i + 1);
		scanf("%d", (V + i));
	}
	S = 0;
	S = suma(n, v);
	printf('%d', S);
	system("pause");
	return 0;
	}
}